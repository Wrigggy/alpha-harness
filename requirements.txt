# Core
torch
numpy
pandas
pyarrow
scipy
scikit-learn

# Binance API (crypto data)
python-binance
aiohttp

# Config & Logging
pyyaml
tqdm
loguru

# RL (AlphaGen / AlphaQCM)
gymnasium
sb3_contrib>=2.0
stable_baselines3>=2.0
shimmy
tensorboard

# Evaluation & Visualization
matplotlib
seaborn
tbparse           # parse tensorboard event files into pandas (for ablation notebook)
nbconvert         # auto-generate HTML report from analysis notebook

# LLM clients (pick whichever backends you'll actually use)
claude-agent-sdk    # backend=claude_code (default; needs Max-plan subscription, no key)
anyio               # async runtime for claude-agent-sdk
openai>=1.0         # backend=openrouter (needs OPENROUTER_API_KEY)
anthropic           # backend=anthropic (needs ANTHROPIC_API_KEY)

# Qlib (optional — for CSI500 data)
# pip install pyqlib
# python -m qlib.run.get_data qlib_data --target_dir ~/.qlib/qlib_data/cn_data --region cn

# Jupyter
jupyter
ipykernel
