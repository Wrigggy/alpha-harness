# Core
torch
numpy
pandas
pyarrow
scipy
scikit-learn

# Binance API (crypto data)
python-binance
aiohttp
akshare
baostock
yfinance

# Config & Logging
pyyaml
tqdm
loguru

# RL (AlphaGen / AlphaQCM)
gymnasium
sb3_contrib>=2.0
stable_baselines3>=2.0
shimmy
tensorboard

# Evaluation & Visualization
matplotlib
seaborn

# LLM Judge (Max plan — choose one)
claude-agent-sdk    # Optional: Claude Max / agent SDK path
anthropic           # Optional: Anthropic API
openai              # OpenAI API and DeepSeek-compatible client path
anyio               # Async support for agent SDK

# Qlib (optional — for CSI500 data)
# pip install pyqlib
# python -m qlib.run.get_data qlib_data --target_dir ~/.qlib/qlib_data/cn_data --region cn

# Jupyter
jupyter
ipykernel
